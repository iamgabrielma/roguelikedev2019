using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//using System.Diagnostics;
//using System;

public class wip_h_MethodAnalysis : MonoBehaviour
{
    private void Start()
    {
        Debug.Log("Hello from h_");   
    }
    // Constructor: It takes a method as an argument via a "delegate", so we can analyze how long a function takes
    //public static h_MethodAnalysis(Func<string, int> _MethodName) {

    //    // Do stuff
    //    int i = _MethodName("My String");
    //    // Do more stuff

    //}
    //Stopwatch stopwatch = new Stopwatch();
    //Stopwatch stopwatch = new Stopwatch();
    //stopwatch.Start();
    //FloorSetup(map_width, map_height);
    //stopwatch.Stop();
    //TimeSpan ts = stopwatch.Elapsed;
    //int _ms = ts.Milliseconds;

}
